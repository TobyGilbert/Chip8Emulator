#include <iostream>
#include "emulator.hpp"

// Brains of the emulator for pocessing opcodes
chip8emu::Emulator emulator;

int main()
{
    std::cout << "Initialising Emulator." << std::endl;

    // Setup render system and register input callbacks

    // Initialise the Chip8 system and load the game into memory

    // Emulation loop
    // while (true)
    // {
    // Emulate one cycle

    // If the draw flag is set, update the screeb

    // Store the key press state
    // }

    return 0;
}